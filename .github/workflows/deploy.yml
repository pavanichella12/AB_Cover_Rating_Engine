# CI/CD: On push to main, build Docker image, push to ECR, and update ECS service.
# Runs only when app code or config changes (not on every push). You can also "Run workflow" manually.
name: Build and Deploy to ECS

on:
  push:
    branches:
      - main
    paths:
      - 'app.py'
      - 'auth.py'
      - 'audit.py'
      - 'add_user.py'
      - 'agents/**'
      - '.streamlit/**'
      - 'docker/**'
      - 'requirements.txt'
      - 'abcover_logo.png'
  workflow_dispatch:  # Deploy anytime: Actions → Build and Deploy to ECS → Run workflow

env:
  AWS_REGION: us-east-1
  ECR_PUBLIC_REGISTRY: public.ecr.aws/g8m5c8i1
  ECR_IMAGE: public.ecr.aws/g8m5c8i1/abcover
  ECS_CLUSTER: abcover-cluster
  ECS_SERVICE: abcover-app-service-xnu4i50r

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      # Fail fast with a clear message if secrets are missing (no values are printed)
      - name: Check AWS secrets are set
        run: |
          if [ -z "${{ secrets.AWS_ACCESS_KEY_ID }}" ]; then echo "::error::Repository secret 'AWS_ACCESS_KEY_ID' is not set. Add it in Settings → Secrets and variables → Actions."; exit 1; fi
          if [ -z "${{ secrets.AWS_SECRET_ACCESS_KEY }}" ]; then echo "::error::Repository secret 'AWS_SECRET_ACCESS_KEY' is not set. Add it in Settings → Secrets and variables → Actions."; exit 1; fi
          echo "Secrets are set."

      - name: Configure AWS credentials
        uses: aws-actions/configure-aws-credentials@v4
        with:
          aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws-region: ${{ env.AWS_REGION }}

      - name: Login to Amazon ECR Public
        run: |
          aws ecr-public get-login-password --region ${{ env.AWS_REGION }} | \
          docker login --username AWS --password-stdin ${{ env.ECR_PUBLIC_REGISTRY }}

      - name: Build, tag, and push image
        run: |
          docker build --platform linux/amd64 -f docker/Dockerfile -t ${{ env.ECR_IMAGE }}:latest .
          docker push ${{ env.ECR_IMAGE }}:latest

      - name: Deploy to ECS
        run: |
          aws ecs update-service \
            --cluster ${{ env.ECS_CLUSTER }} \
            --service ${{ env.ECS_SERVICE }} \
            --force-new-deployment \
            --region ${{ env.AWS_REGION }}
